spring.application.name=mural-criando-o-futuro

server.port=${PORT:8090}
# PostgreSQL Database Configuration (with Supabase connection pooler)
spring.datasource.url=jdbc:postgresql://aws-0-us-west-1.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0
spring.datasource.username=postgres.cegmxoauvphewmbdqqku
spring.datasource.password=!HvgTie7izWbMUT
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect


# Hibernate C3P0 Configuration
spring.jpa.properties.hibernate.c3p0.max_size=20
spring.jpa.properties.hibernate.c3p0.min_size=5
spring.jpa.properties.hibernate.c3p0.timeout=300
spring.jpa.properties.hibernate.c3p0.idle_test_period=300

# JPA Configuration (Automatic schema updates)
spring.jpa.hibernate.ddl-auto=update

# Thymeleaf Configuration
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8

# Configura o tamanho máximo do arquivo (por exemplo, 20MB)
spring.servlet.multipart.max-file-size=20MB

# Configura o tamanho máximo da requisição inteira (por exemplo, 20MB)
spring.servlet.multipart.max-request-size=20MB


